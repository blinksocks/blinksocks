'use strict';var _SelectMessage=require('../SelectMessage');var _common=require('../../common');describe('SelectMessage#toBuffer',function(){it('should return the expected buffer',function(){var message=new _SelectMessage.SelectMessage;var buffer=Buffer.from([_common.SOCKS_VERSION_V5,_common.METHOD_NO_AUTH]);expect(message.toBuffer().equals(buffer)).toBe(true)})});